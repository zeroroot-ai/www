---
/**
 * The landing page.
 *
 * Composition only: every section is its own component and every word it
 * renders comes from @/lib/messaging, so this file says what the page IS and
 * nothing about what it says.
 */
import Landing from "@/layouts/Landing.astro";
import Hero from "@/components/landing/Hero.astro";
import Stack from "@/components/landing/Stack.astro";
import CheckIn from "@/components/landing/CheckIn.astro";
---

<Landing title="zeroroot · the zero-trust agent runtime">
  <Hero />
  <Stack />
  <CheckIn />
</Landing>
